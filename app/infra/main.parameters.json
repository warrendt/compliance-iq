{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION=swedencentral}"
    },
    "openAiModelName": {
      "value": "${AZURE_OPENAI_MODEL_NAME=gpt-5.2}"
    },
    "openAiModelVersion": {
      "value": "${AZURE_OPENAI_MODEL_VERSION=2026-02-01}"
    },
    "openAiFallbackModel": {
      "value": "${AZURE_OPENAI_FALLBACK_MODEL=gpt-5.4-mini}"
    },
    "openAiFallbackVersion": {
      "value": "${AZURE_OPENAI_FALLBACK_VERSION=2026-04-01}"
    },
    "devPublicIpAddress": {
      "value": "${DEV_PUBLIC_IP=}"
    }
  }
}
